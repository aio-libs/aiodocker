machine:
    services:
        - docker
    python:
        version: 3.6.0
dependencies:
    override:
        - pip install -e '.[test]'
test:
    override:
        - flake8 aiodocker tests
        - pytest tests
