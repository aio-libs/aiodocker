machine:
    services:
        - docker
    python:
        version: 3.6.1
    post:
      - pyenv global 3.5.1 3.5.3 3.6.1

dependencies:
    override:
        - pip install -e '.[test]'
test:
    override:
        - flake8 aiodocker tests
        - pytest tests
