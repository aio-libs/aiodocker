``images.pull()`` (and its ``Docker.pull`` alias) no longer triggers the Docker Engine's "pull all tags" behavior when ``tag`` is omitted and ``from_image`` has no embedded tag or digest; it now pulls ``"latest"`` instead. Callers that relied on the previous all-tags behavior must pass ``tag=""`` explicitly to opt back in.
